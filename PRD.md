# 智能旅游规划产品轻量 PRD

> 版本：v0.4  
> 状态：产品范围已确认  
> 首期方向：国内多日自驾旅行规划  
> 只读原型参考：[现有新疆自驾地图](/Users/felixcui/Documents/tmp/xinjiang-roadtrip-map/index.html)  
> 产品代码、数据、测试和后续文档统一在 `travel-planner` 目录维护

## 1. 产品目标

用户输入目的地、游玩天数、同行人员和偏好后，系统生成一套可执行、可编辑、可导航的每日旅游计划，并通过“每日卡片 + 地图路线 + 行程详情抽屉”展示。

产品重点解决：

- 景点顺序是否合理，是否存在折返。
- 每天驾驶、游玩和休息时间是否可完成。
- 家庭、儿童、老人等不同成员能否承受当天强度。
- 修改景点或住宿后，如何快速重新计算受影响的行程。

## 2. 首期范围

### 2.1 包含

- 用户可输入任意国内城市、省份或连续旅游区域，系统生成多日自驾行程。
- 需求采集与行程方案生成。
- 1～3 套不同侧重的候选方案。
- 每日路线、分段距离、预计车程和住宿区域。
- 景点特点、推荐玩法、建议停留时间和注意事项。
- 地图总览、按天聚焦及当天详情抽屉。
- 增删景点、调整顺序、修改住宿和重新计算。
- 无需登录即可创建和保存行程，并支持分享链接、Excel/PDF 导出。
- 一期服务端数据使用文件存储，后续可平滑迁移到数据库。
- 地图服务可替换：首期 OSM/OSRM，后续接入高德或百度。

### 2.2 暂不包含

- 到达目的地或离开目的地的飞机、火车等大交通规划。
- 包车、公共交通和步行换乘方案。
- 酒店、机票、门票的实时价格和直接预订。
- 实时路况导航、导航播报及车辆轨迹记录。
- 多人协作、社区攻略和商业推广排序。
- 跨国行程及公共交通换乘规划。

## 3. 目标用户

首期主要面向计划国内多日自驾的个人和家庭用户，尤其关注：

- 带儿童或老人的家庭。
- 不熟悉目的地、需要完整路线建议的用户。
- 希望控制每日驾驶强度和出发时间的用户。
- 已有必去景点，但不知道如何合理串联的用户。

## 4. 用户输入

### 4.1 必填信息

| 信息 | 说明 |
| --- | --- |
| 目的地 | 城市、省份或连续旅游区域，例如“新疆伊犁” |
| 游玩天数 | 只计算目的地内的完整游玩天数 |
| 同行人员 | 成人、儿童、老人数量及儿童年龄段 |
| 旅行节奏 | 轻松、适中、紧凑 |
| 必去地点 | 至少可为空，由系统推荐 |

### 4.2 建议补充信息

- 具体旅行日期或月份。
- 当地行程起点和最终结束地点。
- 每天最早出发、最晚结束时间。
- 兴趣偏好：自然、人文、亲子、美食、摄影、徒步等。
- 不想去的地点或不接受的活动。
- 单日最长驾驶时间、最长连续驾驶时间。
- 已确定的住宿地点、住宿预算和房型偏好。
- 徒步能力、高原适应、饮食及无障碍需求。

系统采用渐进式提问：先收集必填信息，只有在影响方案可行性时才追问补充信息。

## 5. 核心用户流程

1. 用户输入目的地、天数、人员和偏好。
2. 系统补问影响路线的关键约束。
3. 系统生成 1～3 套候选方案，例如“轻松亲子”“经典景点”“深度体验”。
4. 用户选择方案，进入地图行程页。
5. 用户点击每日卡片，查看当天时间安排、分段导航和详细玩法。
6. 用户增删地点、拖动排序或调整住宿。
7. 系统重新计算受影响日期，并提示超时、折返或强度过高。
8. 用户保存、复制、分享或导出行程。

## 6. 核心功能

| 优先级 | 功能 | 要求 |
| --- | --- | --- |
| P0 | 创建行程 | 根据基础输入创建一份结构化旅行计划 |
| P0 | 生成候选方案 | 输出不同节奏或主题的方案，并解释差异 |
| P0 | 地图路线 | 展示全程和每日路线、地点名称、分段距离与车程 |
| P0 | 每日详情 | 展示当天安排、路线段、玩法、停留时间、住宿和提醒 |
| P0 | 行程校验 | 检查开放时间、每日可用时间、驾驶强度、折返和住宿落点 |
| P0 | 编辑与重算 | 增删、排序地点或修改住宿后，只重算受影响的日期 |
| P0 | 匿名保存 | 无需登录，保存用户输入、方案版本和编辑结果 |
| P0 | 导出分享 | 生成匿名分享链接、Excel 和 PDF |
| P0 | 景点数据采集 | 从合规网络来源采集景点资料并记录来源、时间和数据状态 |
| P0 | 运营内容管理 | 支持景点新增、修正、下线、合并、版本记录和回滚 |
| P1 | 方案复制 | 从现有方案复制出新版本，保留原方案 |
| P1 | 备用方案 | 对天气、闭园或道路不可用提供替代地点和路线 |

## 7. 行程展示

### 7.1 左侧每日卡片

卡片保持简洁，只显示：

- 第几天和当天主题。
- 地点顺序。
- 当天总里程与预计自驾时间。
- 当晚住宿区域。
- 行程强度标识：轻松、适中、较累、不建议。

### 7.2 地图区域

- 默认展示完整路线。
- 点击每日卡片后，只突出当天地点和路线。
- 路线段标记预计时间，地点名称常驻显示。
- 点击地点显示特点、玩法、停留时间、预约和注意事项。

### 7.3 当天详情抽屉

- 当天总里程、总驾驶时间、地点数量和强度。
- 按时间轴展示出发、驾驶、游玩、用餐、休息和入住。
- 每段“起点 → 终点”的距离、车程和导航入口。
- 每个地点的推荐玩法、建议停留时间和适合人群。
- 当天住宿区域及选择原因。
- 当天风险和备用建议。

## 8. 行程生成与校验原则

系统区分硬约束和软偏好。

### 8.1 硬约束

- 旅行天数、每天可用时间。
- 景点开放日期和开放时间。
- 必去地点及已确定的住宿。
- 道路是否可通行。
- 用户设置的最长驾驶或徒步限制。

### 8.2 软偏好

- 兴趣和景点类型。
- 旅行节奏与每日景点数量。
- 少折返、少换酒店、景观路线优先。
- 儿童、老人友好程度。
- 住宿预算和区域偏好。

地图服务负责距离和路线计算，规则引擎负责时间及约束校验，AI 负责理解需求、推荐候选地点和生成易读说明。AI 不直接编造距离、驾驶时间、开放时间或门票信息。

## 9. 地图服务方案

### 9.1 首期实现

- 地图底图与地点数据：OpenStreetMap。
- 地理搜索：基于 OSM 数据的地理编码服务。
- 驾车路线、距离和时间：OSRM。
- 无法获得道路路线时，显示明确标识的估算路线，不伪装成精确导航结果。

公开 OSM/OSRM 服务只用于原型和低频试用。进入正式运营前，需要评估自建服务或商业服务的稳定性、配额、使用政策和成本。

### 9.2 可替换接口

业务层不得直接调用某一家地图服务，统一通过 `MapProvider` 适配层访问：

```text
MapProvider
├── geocode(query, region)
├── reverseGeocode(location)
├── searchPlaces(query, bounds, category)
├── calculateRoute(points, options)
├── calculateMatrix(origins, destinations, options)
└── normalizeLocation(providerLocation)
```

统一返回：

- 标准地点 ID、名称、地址和分类。
- WGS84 经纬度作为系统内部标准坐标。
- 路线距离、预计时间、路线几何和分段信息。
- 服务提供商、计算时间、是否为估算值和错误状态。

后续接入高德或百度时，由对应适配器处理鉴权、字段映射、坐标系转换、配额和错误码，行程规划及前端展示逻辑不需要改写。

## 10. 景点数据策略

景点资料采用“网络采集为主、运营维护为辅”的组合方式。

### 10.1 网络数据采集

- 从允许使用的公开网页、景区官方渠道和开放数据源采集景点基础资料。
- 采集名称、位置、分类、特点、玩法、建议停留时间、开放时间、预约要求和注意事项。
- 每条数据保存来源链接、来源名称、采集时间和最近校验时间。
- 对多个来源进行合并和冲突标记，不直接用未经校验的新数据覆盖已有可信内容。
- 遵守来源网站的服务条款、robots 规则、版权要求和访问频率限制。

### 10.2 运营维护

- 运营人员可以新增、编辑、下线和合并景点。
- 可修正坐标、名称、玩法、开放时间、适合人群及季节性提示。
- 运营确认的数据优先级高于自动采集数据；重新抓取时不得直接覆盖人工修改字段。
- 所有人工修改保留版本、修改人、修改时间和回滚记录。

### 10.3 数据状态

景点数据至少区分：待校验、自动采集、运营确认、已过期和已下线。行程生成默认优先使用运营确认且更新时间较新的内容；数据不完整时允许使用自动采集内容，但必须标明来源和风险。

## 11. 核心数据结构

```text
Trip
├── 用户需求与约束
├── Plan[]                         候选或复制出的方案
│   ├── Day[]
│   │   ├── Activity[]             景点、用餐、休息、住宿
│   │   ├── RouteSegment[]         分段距离、时间、路线与导航
│   │   ├── Stay                   住宿区域
│   │   └── ValidationIssue[]      超时、折返、闭园、强度等问题
│   └── PlanVersion
└── 当前选中方案
```

用户修改后的方案必须作为独立版本保存，不能覆盖原始生成方案。

## 12. 数据存储策略

### 12.1 一期文件存储

- 浏览器本地存储匿名用户尚未分享的行程草稿。
- 服务端使用结构化 JSON 文件保存景点资料、运营修改记录、每次成功生成的匿名行程、分享快照和必要的索引信息。
- 不把路线几何、采集原文等大体积或可重新生成的数据重复写入核心行程文件；可单独缓存并设置失效时间。
- 每类文件都包含 `schemaVersion`、唯一 ID、创建时间、更新时间和数据来源。
- 文件写入采用临时文件写入后原子替换，避免进程中断产生半个 JSON 文件。
- 定期备份数据目录；运营修改和行程版本使用追加式历史记录，避免直接覆盖后无法恢复。

建议的逻辑结构：

```text
data/
├── places/              景点和运营修订数据
├── trips/               匿名行程与版本
├── shares/              分享标识与只读版本映射
├── sources/             采集来源及更新时间
└── cache/               可失效的路线和抓取缓存
```

### 12.2 存储适配层

业务代码不得直接依赖文件路径，通过统一仓储接口访问数据：

```text
TripRepository
├── create(trip)
├── getById(id)
├── saveVersion(tripId, version)
└── getSharedVersion(shareId)

PlaceRepository
├── search(query, filters)
├── getById(id)
├── saveCollected(place)
└── saveOperatorOverride(placeId, changes)
```

一期实现 `FileTripRepository` 和 `FilePlaceRepository`；后续增加数据库实现时，行程规划、地图和页面接口保持不变。

### 12.3 数据库迁移准备

- JSON 数据结构与领域模型保持一致，禁止用文件名表达关键业务关系。
- 所有数据带版本号，并提供文件完整性检查和导出脚本。
- 迁移数据库时先双读校验，再一次性切换写入，文件保留为可回滚备份。
- 当出现并发写入冲突、数据量明显增长、复杂检索或多实例部署需求时，启动数据库迁移。

## 13. 行程强度规则（首版）

首版使用可配置规则给出提示，不作为绝对安全结论：

- 轻松：驾驶不超过 3 小时，主要景点不超过 2 个。
- 适中：驾驶 3～5 小时，游玩时间仍有充足缓冲。
- 较累：驾驶超过 5 小时，或长途驾驶与重度游玩叠加。
- 不建议：预计结束时间超出用户限制，或关键景点无法在开放时间到达。

儿童、老人同行时降低连续驾驶和连续高强度天数阈值，并增加用餐、卫生间和机动缓冲。

## 14. 非功能要求

- 地图供应商可通过配置切换，业务数据不绑定供应商专有格式。
- 路线计算失败时能够降级，并清楚标注估算状态。
- 保存每次路线计算的来源和时间，避免把历史结果当作实时信息。
- 用户编辑后优先局部重算，不阻塞其他日期浏览。
- 桌面端和移动端均可完成创建、查看、编辑和关闭详情操作。
- 用户输入、位置和行程默认私有，分享必须由用户主动开启。
- 第一版不要求登录；浏览器本地保存最后草稿，每次成功生成的行程自动保存到服务端并进入“我的行程”列表；分享时另行创建不可猜测的只读快照。
- 匿名分享页面默认只读，不暴露设备标识、编辑历史或其他未分享方案。
- 文件存储必须通过仓储适配层访问，页面和行程规划逻辑不得读取具体文件路径。
- 服务端文件写入需要原子性、数据校验和备份，失败时不得破坏上一版本。

## 15. MVP 验收标准

- 用户可以通过不超过两轮表单或问答完成基础需求输入。
- 系统能够生成至少一套完整多日自驾方案。
- 每天都有地点顺序、预计到达/离开时间、游玩内容、住宿区域和强度提示。
- 所有相邻地点都有分段距离和预计车程，且标明数据来源或估算状态。
- 点击每日卡片可在地图上聚焦当天路线并打开详情抽屉。
- 用户增删或调整地点后，路线、时间轴和强度提示会同步更新。
- OSM/OSRM 不可用时页面不崩溃，并显示可理解的降级状态。
- 更换测试版 `MapProvider` 时，不需要修改行程领域模型和页面组件。
- 行程可被保存并重新打开，原始方案和用户编辑版本均可追溯。
- 每次生成成功后无需额外操作即可在“我的行程”列表中查看并重新打开，后续编辑会更新保存记录。
- 用户不登录也能完整创建、编辑和本地保存行程。
- 用户可以主动生成只读分享链接，并分别导出结构正确的 Excel 和 PDF。
- 用户输入任意国内目的地时，系统能够返回结果或明确说明数据不足，不能静默生成虚假地点。
- 自动采集的景点信息包含来源和更新时间，运营修改后重新采集不会覆盖人工字段。
- 重启服务后，文件中保存的景点、运营修改、匿名分享和行程版本仍能正确读取。
- 模拟写入失败时，原有 JSON 文件保持完整且可以继续读取。
- 用内存版或测试数据库版 Repository 替换文件实现后，核心业务测试无需修改。

## 16. 首期风险

- OSM 地点信息、开放时间和景点分类可能不完整。
- 公共 OSRM/Nominatim 服务不适合作为正式产品的稳定依赖。
- 中国地图存在 WGS84、GCJ-02、BD-09 坐标差异，适配错误会造成路线与标记偏移。
- 道路管制、天气、季节开放和预约状态变化快，必须标注数据时间并允许人工修正。
- AI 生成内容可能看似合理但无法执行，所有关键时间与路线必须经过规则和地图计算校验。
- 不同目的地的数据完整度差异较大，需要对低覆盖区域明确降级并支持运营补录。
- 网络采集涉及来源授权、版权、更新频率和反爬限制，上线前需要完成来源白名单及合规检查。
- 文件存储不适合高并发写入、多实例部署和复杂查询；一期需要限制并发场景并监控数据规模。
- 直接人工编辑 JSON 容易破坏结构，运营操作必须经过管理界面或带校验的工具完成。

## 17. 已确认产品决策

1. MVP 只支持自驾，不支持包车或公共交通方案。
2. 不预设首批目的地，用户可以输入任意国内目的地。
3. 第一版不提供账号登录，采用匿名创建和保存。
4. 第一版包含分享链接、Excel 和 PDF 导出。
5. 景点资料以网络采集为主，同时支持运营新增、修正、下线和版本回滚。
6. 一期使用文件存储，业务层通过 Repository 适配；后续根据并发、数据量和检索需求迁移到数据库。
7. 景点资料默认由系统在规划时自动搜索；一期使用 Tavily Search/Extract，并通过 `SearchProvider` 保留替换能力，运营后台主要承担纠错、确认和回滚。
